<h1>Login App</h1>
<div class="row">
    <div class="col-12">
        Hello ! Welcome to this login app
    </div>
    <% if @user != nil%>
        <div class="col-12">
            <%= @user.name  %> You have logged in, if you want to log out, please <a href='/users/logout'>click me</a>
        </div>
    <% else %>
        <div class="col-12">
            <small class="font-weight-lighter">If you have account....</small>
        </div>
        <p id="notice" class="col-12 error"><%= notice %></p>
        <form class="col-12" id="login-form" action="/users/login" accept-charset="UTF-8" method="post" enctype="multipart/form-data">
            <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
            <div class="form-group" >
                <label for="exampleInputEmail1">Email address</label>
                <input name="email" type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="Enter email" required>
            </div>
            <div class="form-group">
                <label for="exampleInputPassword1">Password</label>
                <input name="password" type="password" class="form-control" id="exampleInputPassword1" placeholder="Password" required>
            </div>
            <div class="form-group">
                <a href="/passwords/new">forget your password?</a>
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
        <hr class="col-12">
        <div class="col-12">
            <small class="font-weight-lighter">But if not...</small><br/>
            <a href="/users/new">Sign up now!</a>
        </div>
    <% end %>

</div>